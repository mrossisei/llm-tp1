# torch se instala aparte (ver README: CPU con el index de CPU, GPU sin el index)
pandas
numpy
scikit-learn
matplotlib
# solo para el experimento de transfer learning (los modelos preentrenados se bajan de Hugging Face):
#   sentence-transformers   -> eda/embed_titulos.py (embeddings congelados del titulo)
#   transformers            -> btr.train --text-emb-finetune (fine-tuning, GPU)
